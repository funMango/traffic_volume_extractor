# AGENTS.md

## 작업 지침

1. `.env` 파일은 절대로 읽지 않는다.
2. 데이터베이스는 오직 읽기 전용으로만 접근한다.
3. 데이터베이스에 대해 생성, 수정, 삭제, 마이그레이션, 초기화, truncate, drop 등 쓰기 작업은 절대로 수행하지 않는다.
4. 교통량을 추출할 때는 데이터베이스에 직접 접근하지 않고, 반드시 API를 통해 간접적으로 접근해서 추출한다.

## 터미널 사용 지침

1. 파일 읽기: `cmd /c type "path"`
2. 파일 목록: `cmd /c dir /b "path"`
3. Python 가능 여부 확인 및 스크립트 실행: `cmd /c "python ..."`
4. 긴 검색은 피하고, 필요하면 `cmd /c findstr /n /c:"패턴" "파일"`처럼 단일 파일 대상만 사용한다.
5. 파일 수정은 여전히 `apply_patch`를 사용한다.
