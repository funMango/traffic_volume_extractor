# 2026년 7월 작업 로그

## 2026-07-22 (스마트교차로 전체 일평균 원인 및 주말 평균 확인)

- `S_CRSRD_ACSR_TRF_1HH`를 읽기 전용으로 재조회해 토·일 기준 주말 일평균을 5월 10일, 6월 8일 분모로 계산했다.
- 기존 `fill_samjeong_remicon_sheet2_smart.py`의 전체 일평균 계산이 기간을 거르지 않고 5~6월 전체 시간합계를 사용한 뒤 각각 31일·30일로 나누는 문제를 확인했다. 5월·6월 전체 일평균이 서로 약 31:30 비율인 것은 동일한 두 달 합계가 사용됐다는 검증 근거다.
- 평일·평일 첨두시 계산은 대상 날짜를 명시적으로 필터링하므로 위 기간 누적 문제의 영향을 받지 않는다. 통합문서는 변경하지 않았다.

## 2026-07-22 (스마트교차로 일평균 산식 점검)

- `삼정동_레미콘_교통량_수정_v1.xlsx`의 `스마트교차로 비교` 시트를 읽기 전용으로 점검했다. 기존 실행기는 전체 일평균을 5월 31일·6월 30일, 평일 일평균을 공휴일 제외 5월 18일·6월 21일로 나누며, 첨두시는 평일 07·08·17·18시만 같은 평일 분모로 나눈다.
- 박촌교 삼거리 남향의 현재 값은 5월 전체 29,832·평일 19,278, 6월 전체 30,826·평일 16,733이다. 반올림된 값으로 역산하면 평일 제외일의 평균은 각각 약 44,445·63,710으로 평일보다 높아, 현재 전체 평균 우세는 집계 산식 자체가 아니라 원천의 주말·공휴일 시간합계가 큰 결과다.

## 2026-07-22 (삼정동 레미콘 8개 지점 위치 지도)

- 갱신된 `00_Data\\삼정동_레미콘\\삼정동_위경도.xlsx`의 14개 카메라 좌표를 읽어, 방향별 카메라가 있는 지점은 실제 위도·경도의 평균으로 통합했다.
- `박촌교 삼거리`, `봉오고가교사거리`, `삼정고가삼거리`, `삼정교사거리`, `산업길사거리`, `봉오대로사거리`, `자동차검사소`, `삼정동 320-1` 순서의 8개 지점을 생성하도록 위치 지도 생성기를 확장했다.
- 필수 엑셀 헤더, 좌표 범위, 지정 지점 누락·카메라 종류·도로 불일치, 지정되지 않은 원본 카메라를 검증하고, HTML에서 1~8번·지점 순서·타일·팝업·범례·근접 좌표 보정 요소를 자동 검증했다.
- `02_Result\\삼정동_레미콘_8개_지점_위치지도.html`을 생성하고, `py_compile` 및 생성 후 JSON 데이터 검증으로 8개 번호·명칭·카메라 종류와 실제 좌표를 확인했다. 브라우저 연결은 환경 메타데이터 오류로 수행하지 못했다.
- 지도 생성기에 `ruff format --check` 및 `ruff check`를 실행해 포맷과 정적 검사를 통과했다.

## 2026-07-21 (삼정동 레미콘 교통량 수식 제거)

- 스프레드시트 작업 지침과 프로젝트 터미널·Git 규칙을 확인하고, 대상 원본이 `02_Result\09_기타\삼정동_레미콘_교통량.xlsx`에 있음을 확인했다.
- 월간 작업 로그의 기록 방식과 현재 7월 로그 파일을 확인했다.
- 원본의 Open XML 워크시트 17개를 점검한 결과 수식 셀은 0개이며, 수식 캐시 누락도 없음을 확인했다. 따라서 현재 지정 원본에는 변환할 수식이 없다.
- OneDrive 바탕화면 경로가 존재하고, 만들 결과 파일명은 아직 사용 중이지 않음을 확인했다.
- 원본을 `C:\Users\An JaeYeol\OneDrive\Desktop\삼정동_레미콘_교통량_값만.xlsx`로 복사했고, SHA-256 해시가 원본과 일치함을 확인했다.
- 최종 사본을 다시 점검해 수식 셀 0개, 시트 수 17개, 시트 이름·순서가 원본과 동일함을 확인했다. 해시 일치로 숫자 표시 형식·색상·테두리·열 너비·병합을 포함한 통합문서 내용도 원본과 동일하다.
- Git 작업트리에 이번 작업과 무관한 기존 변경이 다수 있음을 확인했다. 기존 변경을 포함하지 않기 위해 이번 로그 변경은 커밋·push하지 않는다.

## 2026-07-21 (스마트교차로 평일 첨두시 평균 수정)

- `S_CRSRD_TRF_1HH`를 읽기 전용 트랜잭션으로 재집계해, 토·일과 지정 공휴일을 제외한 07·08·17·18시 통행량의 일합계를 계산했다.
- `삼정동_레미콘_교통량_v2.xlsx`의 `스마트교차로 비교` 시트에서 스마트교차로(대) J4:J15만 갱신했다. 5월은 18일, 6월은 20일의 고정 분모를 적용하고 소수점 이하는 버렸다.
- 누락 또는 0 통행 시간대는 0으로 포함하며 기존 시간대 품질 필터와 반올림을 적용하지 않도록 회귀 테스트를 보강했다.
- 저장 후 J4:J15의 DB 재집계 값과 `#,##0` 표시 형식을 확인하고, 다른 모든 셀의 값·수식이 변경되지 않았음을 스냅샷 비교로 검증했다.

## 2026-07-21 (Samjeong remicon grouped log sheets)

- Added eight source-log sheets to `삼정동_레미콘_교통량_v2.xlsx` while retaining `작성서식` and `월별 일평균`; the date range is 2026-05-01 through 2026-06-30.
- Queried SQLite in `mode=ro`; only the two specified exact remicon plate formats were written, grouped by the requested locations and sorted by `collected_at`, `id` (51,567 rows total).
- The 11 source columns, freeze panes, filters, date display format, source-row reconciliation, period, location, plate-format, and ordering checks pass with `--verify-only`.
- Excel prohibits ASCII `*` in sheet names, so the visible-compatible name is `자동차검사소＊`.

## 2026-07-07

- `01_Program\00_main\차종별_교차로_추출.py` 실행 시 DB 환경변수가 로드되지 않는 원인을 확인했다.
- 스크립트는 `01_Program\00_Data\.env`를 참조하지만 실제 `.env`와 `VKND_KIND.json`은 프로젝트 루트의 `00_Data`에 있는 것을 확인했다.
- 루트 `00_Data\.env`에는 `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_SERVICE` 키가 존재하는 것을 값 출력 없이 확인했다.
- `01_Program\00_main\차종별_교차로_추출.py`의 `.env`, `VKND_KIND.json` 참조 경로를 프로젝트 루트 `00_Data` 기준으로 수정했다.
- `.venv\Scripts\python.exe -m py_compile`로 문법 검사를 수행했고, 수정된 경로에서 필수 DB 환경변수 4개가 로드되는 것을 값 출력 없이 확인했다.
- `01_Program\00_main\차종별_교차로_추출.py`의 `RESULT_DIR`을 프로젝트 루트 `02_Result\차종별_교통량_추출` 기준으로 수정했다.
- `.venv\Scripts\python.exe -m py_compile 01_Program\00_main\차종별_교차로_추출.py`로 문법 검사를 수행했다.

## 2026-07-08

- `01_Program\00_main\공공데이터.py`의 `.env` 및 출력 폴더 기준 경로가 `01_Program`으로 잡히는 문제를 확인했다.
- `BASE_DIR`을 프로젝트 루트 기준으로 수정해 `00_Data\.env`와 `02_Result\공공데이터`를 참조하도록 변경했다.
- DB 환경변수 누락 오류 메시지에 확인한 `.env` 경로를 포함하도록 수정했다.
- `Test-Path .\00_Data\.env`로 `.env` 파일 존재 여부만 확인했고, `.env` 내용은 열람하지 않았다.
- `.venv\Scripts\python.exe` import 검증으로 `ENV_PATH`와 `OUTPUT_DIR`이 프로젝트 루트 기준 경로로 계산되는 것을 확인했다.
- `.venv\Scripts\python.exe -m py_compile .\01_Program\00_main\공공데이터.py`로 문법 검사를 수행했다.
- 2026-07-08 18:16 교통량 보정 병목 확인을 위해 `docs\rules` 문서, `traffic_api` 라우트, UseCase, Oracle gateway, profiling, DRCT baseline cache 코드를 조회했다.
- `TRAFFIC_PROFILE` 실행 로그는 현재 `01_Program\02_Result` 로그에 남아 있지 않아, 코드상 계측 지점 기준으로 DRCT 보정의 `baseline_rows_query`, `baseline_build`, `target_source_rows_query`를 주요 병목 후보로 정리했다.
- DRCT baseline 조회 개수 산정을 위해 `_build_drct_baseline_fetch_groups`, `_drct_day_bucket`, `domain.anomaly_core.get_day_bucket` 구현을 확인했다.
- corrected-traffic DRCT baseline cache miss 조회에 `TRAFFIC_API_DRCT_BASELINE_PARALLEL_WORKERS`, `TRAFFIC_API_DRCT_BASELINE_GLOBAL_LIMIT` 설정 기반 병렬 worker 경로를 추가했다.
- `C:\tmp\python312-codex\python.exe -m py_compile`로 `api_server.py`, `oracle_traffic_gateway.py`, `test_api_server_drct.py` 문법 검사를 수행했다.
- `C:\tmp\python312-codex\python.exe -m pytest 03_Test\test_api_server_drct.py -q` 및 `03_Test\test_traffic_api_clean_architecture.py -q`를 실행해 통과를 확인했다.
- DRCT baseline 병렬 조회 재측정 결과 `workers=2`가 `1h` 1일, `1h` 7일, `5m` 1일 조건에서 `workers=1`보다 느려 기본값을 `1`로 복구하고, 환경변수 명시 시 `2~8` 병렬 실행은 유지했다.
- 측정은 실행마다 임시 baseline cache를 사용해 cache hit 왜곡을 줄였고, 현재 구현은 group마다 Oracle connection을 열어 병렬화 overhead가 커진다는 결론을 `docs/manual/traffic_correction_develop.md`에 기록했다.

## 2026-07-10

- `.env` DB 접속정보 중복 입력 영향 확인을 위해 `docs/rules` 문서와 현재 DB 접속 코드 경로를 확인했다.
- `.env` 값은 출력하지 않고 키 이름만 점검했으며, `DB_HOST`, `DB_PORT`, `DB_SERVICE`, `DB_USER`, `DB_PASSWORD`, `PORT`, `POLL_INTERVAL_MS`가 각각 2회 정의된 것을 확인했다.
- `python-dotenv` 동작 검증 결과 같은 키가 중복되면 뒤에 나온 값이 최종 적용되는 것을 확인했다.
- `.venv\Scripts\python.exe -m pytest 03_Test/test_traffic_api_clean_architecture.py 03_Test/test_api_server.py 03_Test/test_api_server_drct.py 03_Test/test_api_server_vknd.py`를 실행해 132 passed, 7 failed 결과를 확인했다.
- 2026-07-10 14:41 `api_server.py` 실행 요청에 따라 8000 포트 사용 여부와 `.venv`의 `fastapi`, `uvicorn` import 가능 여부를 확인했다.
- `Start-Process`로 `01_Program\09_else` 작업 디렉터리에서 `.venv\Scripts\python.exe api_server.py`를 백그라운드 실행하고, 로그를 `02_Result\api_server_py.out.log`, `02_Result\api_server_py.err.log`에 연결했다.
- `netstat`와 `curl.exe -sS http://localhost:8000/docs`로 PID 9276이 `0.0.0.0:8000` LISTENING 상태이며 Swagger UI 응답이 반환되는 것을 확인했다.
- 2026-07-10 `/intersections` API 500 원인이 `M_CRSRD_INF` 조회 `ORA-00942`임을 `02_Result\server.log`로 확인했다.
- `traffic_api.infrastructure.oracle_analysis`에서 교차로/접근로/방향코드 기준 테이블 조회가 `ORA-00942`일 때 원천 교통량 테이블 및 `00_Data\교차로_테이블.xlsx`로 fallback하도록 수정했다.
- `03_Test\test_traffic_api_clean_architecture.py`에 기준 테이블 누락 fallback 회귀 테스트를 추가했다.
- `.venv\Scripts\python.exe -m pytest 03_Test\test_traffic_api_clean_architecture.py`, `py_compile`, `ruff format`, `ruff check`를 실행해 통과를 확인했다.
- API 서버를 재시작하고 `curl.exe http://localhost:8000/openapi.json` 200, `curl.exe "http://localhost:8000/intersections?refresh=true"` 200 및 286개 교차로 반환을 확인했다.
- `.env` 수정 반영을 위해 실행 중이던 API 서버 PID 25012를 종료하고 `01_Program\09_else\start_api_server.cmd`로 재시작했다.
- 재시작 후 PID 19352가 8000 포트 LISTENING 상태임을 확인했고, `/openapi.json` 200, `/intersections?refresh=true` 200, `/corrected-traffic-drct` 최소 POST 요청 200 응답을 확인했다.
- 춘의사거리(`BC001N0135`)의 2026년 5월/6월 주말 07시, 08시 원본 기준 교통량을 API `/corrected-traffic-drct`의 `traffic_volume`으로 추출했다.
- 결과를 `02_Result\API_추출\춘의사거리_2026년_5월_6월_주말_0700-0900_원본교통량.xlsx`에 저장하고 `표`, `요약`, `원천` 시트 및 원천 504행 생성을 확인했다.

## 2026-07-13

- `docs/rules` 문서를 확인한 뒤 `00_Data\삼정동_레미콘_데이터.db`를 SQLite 읽기 전용 URI(`mode=ro`)로 여는 추출 스크립트를 추가했다.
- `01_Program\09_else\export_samjeong_remicon_014_vehicle_by_location.py`에서 `vehicle_detection`의 원본 컬럼을 유지하고, `vehicle_number`가 `014`로 시작하며 4번째 문자가 한글인 행만 필터링하도록 구현했다.
- 필터 결과를 `collected_at`, `id` 오름차순으로 정렬하고 `location_name`별 5개 시트로 `02_Result\09_기타\삼정동_레미콘_014차량_location별.xlsx`를 생성했다.
- 생성 결과는 총 8,793건이며 위치별 행 수는 `산업길 사거리[남향]` 16건, `삼정고가 삼거리[동향]` 1,853건, `삼정교 사거리[북동향]` 842건, `삼정동320-1 부천IC` 1,533건, `자동차검사소` 4,549건으로 확인했다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`로 스크립트 문법과 포맷을 검증했고, 저장된 `.xlsx`를 다시 열어 시트 수와 행 수를 확인했다.
- `01_Program\09_else\export_samjeong_remicon_014_vehicle_by_location.py`에 `월평균분석` 시트 생성 로직을 추가했다.
- `월평균분석`은 첫 번째 시트로 생성하고, `신흥로`와 `오정로`별 location 행 및 도로별 `소계` 행을 포함하도록 구성했다.
- 월별 일평균은 `collected_at` 기준 월별 총대수를 해당 월 전체 일수로 나누도록 계산했으며, 2026년 3월은 31일, 2026년 4월은 30일이 적용되는 것을 확인했다.
- `02_Result\09_기타\삼정동_레미콘_014차량_location별.xlsx`를 재생성하고, 기존 location별 5개 시트의 행 수와 월별 총대수, 월일수, 일평균, 도로별 소계가 원본 시트 기준 집계와 일치하는지 검증했다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`로 수정 스크립트 문법과 정적 검사를 통과했다.
- OneDrive Desktop의 `2603_산업길사거리_북향.xlsx`, `2604_산업길사거리_북향.xlsx`, `2603_삼정고가삼거리_서향.xlsx`, `260401_260428_삼정고가삼거리_서향.xlsx`, `260429_260430_삼정고가삼거리_서향.xlsx` 5개 파일을 확인하고 `data_0` 시트 헤더와 총 300,980행을 검증했다.
- `01_Program\09_else\import_samjeong_remicon_north_west_excel.py`를 추가해 5개 `source_file` 기존 행 삭제 후 단일 SQLite 트랜잭션에서 재적재하도록 구현했다.
- 적재 실행 결과 `vehicle_detection` 기존 삭제 0건, `import_summary` 기존 삭제 0건, 신규 삽입 300,980건을 확인했다.
- `vehicle_detection` 총 행 수가 1,313,918건이고 신규 location별 행 수가 `산업길 사거리[북향]` 106,196건, `삼정고가 삼거리[서향]` 194,784건임을 확인했다.
- `01_Program\09_else\export_samjeong_remicon_014_vehicle_by_location.py`의 기대 location 수를 7개로 변경하고 `월평균분석` 도로 매핑에 `산업길 사거리[북향]`, `삼정고가 삼거리[서향]`을 추가했다.
- `02_Result\09_기타\삼정동_레미콘_014차량_location별.xlsx`를 재생성해 014 차량 총 10,551건, location 시트 7개, `월평균분석` 첫 시트 구성을 확인했다.
- 재생성 워크북의 월별 총대수, 월일수, 일평균, 도로별 소계가 원본 location 시트 기준 집계와 일치하는지 검증했다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`로 신규/수정 스크립트 문법과 정적 검사를 통과했다.

- `docs/rules` 문서를 확인한 뒤 `00_Data\삼정동_레미콘_데이터.db`를 SQLite 읽기 전용 URI(`mode=ro`)로만 조회하는 `01_Program\09_else\generate_samjeong_remicon_camera_daily_html.py`를 추가했다.
- `vehicle_detection`의 `location_name`, `substr(collected_at, 1, 10)` 단위 `COUNT(*)` 결과를 2026-03-01부터 2026-04-30까지 집계하고, 동일 차량번호는 제외하지 않는 검지 기록 행 수 기준을 HTML에 명시했다.
- 테이블·필수 컬럼, 7개 카메라, 394개 카메라-일 집계 행, 날짜 범위를 검증하고 누락 날짜를 `null`로 전달해 Plotly 선이 연결되지 않도록 구현했다.
- `02_Result\09_기타\삼정동_레미콘_카메라별_일별_차량대수.html`을 생성했다. 비교용 다중 선그래프와 카메라별 소형 그래프 7개를 포함하며 Plotly 라이브러리는 HTML에 인라인으로 포함된다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`, 생성 스크립트 실행으로 문법·정적 검사·7개 카메라 및 394개 집계 행을 확인했다.

## 2026-07-14 (카메라 위치 지도)

- `docs/rules` 및 스프레드시트 작업 지침을 확인하고, `00_Data\삼정동_위경도.xlsx`의 카메라 위치 지도 생성 작업을 시작함.
- `01_Program\09_else\generate_samjeong_remicon_camera_location_map.py`를 추가해 필수 헤더·7개 행·대한민국 좌표 범위·중복 위치명을 검증하고, 병합형 도로 셀은 직전 도로명을 적용하도록 구현함.
- 35m 이내 좌표를 근접 그룹으로 감지해 그룹 중심 기준 약 20m 방사형 표시 위치를 만들고, Leaflet 마커·실제 위치 점·점선 연결선·팝업·범례가 포함된 `02_Result\09_기타\삼정동_레미콘_카메라_위치지도.html`을 생성함.
- 생성 스크립트 실행 및 HTML 정적 검증으로 7개 마커, 근접 그룹 2개, 보정 마커 4개, Leaflet·OpenStreetMap·자동 범위 설정·점선 연결선·범례 구성을 확인했으며, 결과 HTML은 `02_Result/**` Git 제외 규칙을 적용받음을 확인함.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`로 신규 스크립트 문법과 정적 검사를 통과함. 브라우저 화면 검증은 실행 환경의 브라우저 연결 권한 메타데이터 오류로 수행하지 못함.
- `git commit -m "feat(map): add samjeong camera location map"` 및 `git push`로 관련 변경 2개 파일을 `develop` 원격 추적 브랜치에 반영함.
- 지도 미리보기에서 외부 전역 이미지 스타일이 Leaflet 타일 위치를 깨뜨리는 현상을 확인해, 타일 이미지의 최대 너비·높이를 해제하도록 보정함.
- 수정 뒤에도 동일한 현상이 재현돼 Leaflet CSS 참조의 무결성 해시 불일치로 스타일시트가 차단되는 원인을 확인하고, CSS 참조에서 해당 해시를 제거함.
- 생성 스크립트 재실행과 `py_compile`, `ruff format --check`, `ruff check`를 통과하고 결과 HTML에서 무결성 해시 없는 Leaflet CSS 참조와 Leaflet JavaScript 참조를 확인함.
- 사용자 지정 순서(기존 4→1, 5→2, 7→3, 6→4, 2→5, 3→6, 1→7)에 맞춰 지도 마커 번호를 변경하고 1부터 7까지 각각 한 번만 표시되는지 검증하도록 구현함.
- 지도 아래에 마커 번호와 대응하는 카메라 위치명·도로·카메라 종류를 확인할 수 있는 안내 목록을 추가하고, 생성 HTML에 안내 목록이 포함되는지 검증함.
- 지도 범례에서 실제 위치 연결선 안내 항목을 제거하되, 근접 카메라의 실제 위치와 보정 위치를 잇는 점선은 유지함.

## 2026-07-15 (박촌교 삼거리 5월 SQLite 적재)

- `docs/rules` 및 스프레드시트 작업 지침을 확인하고, 원본 7개 파일의 `data_0` 헤더·실제 행 배치와 SQLite 스키마를 확인함.
- `01_Program\09_else\import_bakchon_bridge_may_excel.py`를 추가해 잘못된 시트 차원 정보를 무시하고 스트리밍으로 읽어, 실제 데이터의 차선·차선방향·위치명 열을 올바르게 매핑하도록 구현함.
- 스키마, 원본 헤더, 파일별 행 수, 위치명, 파일별·위치별·요약·전체 행 수를 검증하고 대상 7개 `source_file`만 삭제 후 단일 SQLite 트랜잭션으로 재삽입하도록 구현함.
- `.venv\Scripts\python.exe -m py_compile`, `ruff check`, `ruff format --check`로 신규 스크립트 문법·정적 검사·포맷을 통과함.
- 적재 실행으로 639,073건을 추가했고, 재실행에서 대상 검지 행 639,073건과 요약 7건만 교체한 뒤 동일 639,073건이 재삽입되는 것을 확인함.
- 읽기 전용 사후 검증으로 파일별 87,262 / 95,388 / 96,703 / 82,649 / 87,907 / 95,388 / 93,776건, 위치별 북향 197,194건·남향 184,120건·서향(순방향) 99,701건·서향(역방향) 158,058건, `import_summary` 7건 및 `vehicle_detection` 총 1,952,991건을 확인함.
- 관련 스크립트와 작업 로그를 `feat(data): import bakchon bridge may records` 커밋(`f036f6d`)으로 `origin/develop`에 반영함.

## 2026-07-15 (박촌교·봉오고가교 원본 SQLite 적재)

- `01_Program\09_else\import_bakchon_bridge_june_excel.py`와 `import_bongo_overpass_intersection_excel.py`를 추가해 원본 Excel을 스트리밍으로 SQLite에 적재하도록 구현함.
- 박촌교 6월 원본은 계획의 8개 표기와 달리 실제로 9개이며, 모두 합산한 744,958건이 기대 건수와 일치함. 파일별·위치별·`import_summary`·전체 행 수를 트랜잭션 안에서 검증하고 재실행 시 해당 `source_file`만 교체하도록 함.
- 봉오고가교 사거리 원본 7개를 적재해 647,351건(남향 511,847건, 서향 135,504건)을 검증함. 재실행에서도 기존 647,351건과 요약 7건을 교체해 동일 건수를 유지함.
- 두 스크립트에 읽기 전용 `--verify-only` 검증을 제공하고, 최종 `vehicle_detection` 3,345,300건을 포함한 검증을 통과함.
- `.venv\Scripts\python.exe -m py_compile`, `ruff check`, `ruff format --check` 및 두 스크립트의 읽기 전용 검증을 통과함.

## 2026-07-15 (6개 교차로·지점 원본 SQLite 적재)

- `docs/rules`와 스프레드시트 작업 지침을 확인하고, `00_Data\2605_06_삼정동_레미콘_데이터` 아래 6개 폴더의 원본 Excel 31개를 대상으로 확인함.
- `01_Program\09_else\import_samjeong_remicon_six_locations_excel.py`를 추가해 봉오대로사거리 13개, 산업길사거리 2개, 삼정고가삼거리 5개, 삼정교사거리 1개, 삼정동320-1부천IC 6개, 자동차검사소 4개 원본을 명시적으로 관리하도록 구현함.
- 원본 `data_0` 시트 헤더와 파일별 기대 행 수를 검증하고, 8열 원본은 실제 행 순서에 맞춰 차선·방향·위치명을 매핑하며 6열 원본은 차선·방향을 빈 값으로 적재하도록 구현함.
- 원본 위치명 전수 스캔으로 봉오대로 사거리 동향·서향, 산업길 사거리 남향·북향, 삼정고가 삼거리 동향·서향, 삼정교 사거리 북동향, 삼정동320-1 부천IC, 자동차검사소만 포함됨을 확인함.
- 최초 적재로 대상 2,434,263건을 추가해 `vehicle_detection` 총 5,779,563건을 확인함. 폴더별 건수는 봉오대로사거리 1,064,333건, 산업길사거리 128,046건, 삼정고가삼거리 390,685건, 삼정교사거리 58,243건, 삼정동320-1부천IC 531,412건, 자동차검사소 261,544건임.
- 위치별 건수는 봉오대로 사거리[동향] 496,241건, 봉오대로 사거리[서향] 568,092건, 산업길 사거리[남향] 19,292건, 산업길 사거리[북향] 108,754건, 삼정고가 삼거리[동향] 190,159건, 삼정고가 삼거리[서향] 200,526건, 삼정교 사거리[북동향] 58,243건, 삼정동320-1 부천IC 531,412건, 자동차검사소 261,544건임.
- 재실행 검증에서 기존 대상 차량 행 2,434,263건과 `import_summary` 31건만 삭제한 뒤 동일 2,434,263건을 재삽입했고, 전체 행 수가 5,779,563건으로 유지됨을 확인함.
- `.venv\Scripts\python.exe -m py_compile 01_Program\09_else\import_samjeong_remicon_six_locations_excel.py`, `ruff check`, `ruff format --check`, `--verify-only` 읽기 전용 검증을 통과함. `ruff` 캐시 파일 쓰기 권한 경고는 있었으나 검사 결과는 통과함.

## 2026-07-15 (삼정동 레미콘 교통량 작성서식 결과 산출)

- `docs/rules` 문서를 확인하고 `00_Data\삼정동_레미콘\삼정동_레미콘_데이터.db`의 `vehicle_detection` 스키마와 `삼정동_레미콘_교통량_작성서식.xlsx`의 `작성서식` 시트 구조를 확인했다.
- `01_Program\09_else\fill_samjeong_remicon_traffic_template.py`를 추가해 SQLite DB를 `mode=ro` 읽기 전용 URI로 열고, 작성서식 8개 지점 행을 방향별 `location_name` 목록으로 합산하도록 구현했다.
- 레미콘 통행량은 `vehicle_number` 정규식 `^014[가-힣]` 기준으로 집계하고, 2026년 5월/6월 월별 전체, 유효 평일, 유효 평일 첨두시(07:00~09:00, 17:00~19:00, 종료 시각 제외) 건수를 작성서식에 입력했다.
- 평일 집계에서 토/일 및 `2026-05-01`, `2026-05-05`, `2026-05-25`, `2026-06-03`을 제외하고, 비율은 `레미콘 통행량 / 총 통행량 * 100`을 소수점 둘째 자리 반올림 값으로 저장했다.
- `.venv\Scripts\python.exe -m py_compile 01_Program\09_else\fill_samjeong_remicon_traffic_template.py`, `ruff check`, `ruff format --check`를 통과했다.
- `.venv\Scripts\python.exe 01_Program\09_else\fill_samjeong_remicon_traffic_template.py`를 실행해 `02_Result\09_기타\삼정동_레미콘_교통량_작성서식_결과.xlsx`를 생성했다.
- 결과 파일을 다시 열어 `작성서식!C4:K19` 빈 값이 없고 숫자 표시 형식이 정수 `#,##0`, 비율 `0.00`인지 확인했다.
- 독립 DB 재집계 검증에서 월별/평일/첨두시 16개 행의 총 통행량, 레미콘 통행량, 비율이 결과 파일과 모두 일치함을 확인했다. 검증 결과 `mismatch_count=0`, `weekend_or_holiday_rows_may_june=1126587`, `weekday_peak_boundary_09_or_19_rows=106`이었다.

## 2026-07-15 (삼정동 레미콘 카메라별 일 단위 누락 구간 산출)

- `docs/rules` 문서를 확인하고, `00_Data\삼정동_레미콘\삼정동_레미콘_데이터.db`를 Python `sqlite3` read-only URI(`mode=ro`)로 열어 `vehicle_detection` 테이블과 `collected_at`, `location_name` 필수 컬럼 존재를 확인했다.
- 2026-05-01부터 2026-06-30까지 61일 달력 날짜를 기준으로 DB 내 15개 `location_name`별 `date(collected_at)` 일별 전체 통행량을 집계하고, 0건 날짜를 연속 구간으로 병합했다.
- 연속 구간 병합 로직은 단일 누락일, 2일 이상 연속 누락, 월 경계 누락, 누락일 없음 케이스로 검증했다.
- 누락 구간은 박촌교 삼거리[남향] 2026-06-20~2026-06-21, 박촌교 삼거리[북향] 2026-06-21, 박촌교 삼거리[서향(순방향)] 2026-06-21, 박촌교 삼거리[서향(역방향)] 2026-06-21, 삼정고가 삼거리[서향] 2026-05-24, 자동차검사소 2026-05-20~2026-05-25 및 2026-06-27~2026-06-28로 확인했다.
- 누락 구간이 없는 카메라는 9개이며, `봉오대로 사거리[북향]`은 DB `location_name`에 없음을 확인했다.

## 2026-07-15 (삼정동 레미콘 카메라별 로그 시트 추가)

- `docs/rules` 문서와 스프레드시트 작업 지침을 확인하고, `00_Data\삼정동_레미콘\삼정동_레미콘_데이터.db`를 SQLite read-only URI(`mode=ro`)로 조회하도록 작업했다.
- `01_Program\09_else\add_samjeong_remicon_camera_log_sheets.py`를 추가해 `vehicle_detection`에서 2026-05-01 00:00:00 이상, 2026-07-01 00:00:00 미만, `vehicle_number`가 `^014[가-힣]`에 맞는 레미콘 실제 기록만 추출하도록 구현했다.
- 정렬은 `location_name ASC, collected_at ASC, id ASC` 기준으로 수행하고, 각 `location_name`별 시트에 `id`, `collected_at`, `registered_at`, `device_id`, `location_name`, `lane`, `lane_direction_name`, `vehicle_number`, `owner_registered_area`, `source_file`, `source_sheet` 원천 확인 컬럼을 기록했다.
- `02_Result\09_기타\삼정동_레미콘_교통량.xlsx`의 기존 `작성서식` 시트는 보존하고, 카메라별 로그 시트 15개를 생성했다. 총 로그 행 수는 20,122건이며 DB 재조회 건수와 일치함을 확인했다.
- 저장 후 워크북을 다시 열어 `작성서식` 존재, 시트 수, 헤더, 각 시트 행 수, 기간, 차량번호 정규식, `collected_at`/`id` 오름차순 정렬을 검증했다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format`, `ruff check`, 생성 스크립트 실행 검증을 통과했다.

## 2026-07-15 (삼정동 레미콘 카메라별 주말집계 시트 추가)

- `docs/rules` 문서를 확인하고 `01_Program\09_else\add_samjeong_remicon_camera_log_sheets.py`를 확장해 `주말집계` 시트를 추가/재생성하도록 수정했다.
- SQLite DB는 `mode=ro` 읽기 전용 URI로 조회하고, `vehicle_detection`의 2026-05-01 00:00:00 이상 2026-07-01 00:00:00 미만 토요일/일요일 행만 `location_name`별로 집계했다.
- `주말 총 통행량`, `주말 레미콘 통행량`, `주말 레미콘 비율(%)` 컬럼을 생성하고 레미콘 차량은 `^014[가-힣]` 정규식으로 판정하도록 구현했다.
- `주말집계` 시트 헤더 고정, 필터, 헤더 강조, 통행량 `#,##0`, 비율 `0.00` 표시 형식과 열 너비 자동 조정을 적용했다.
- `.venv\Scripts\python.exe -m py_compile`, `ruff format --check`, `ruff check`, 생성 스크립트 실행 검증을 통과했고, 저장 후 워크북을 다시 열어 `작성서식`, `주말집계`, 카메라별 로그 시트가 유지되는지 확인했다.
- DB 기준 주말집계는 15개 위치, 주말 총 통행량 896,205건, 주말 레미콘 통행량 71건으로 확인했다.

## 2026-07-16 (Notion MCP OAuth 연결)

- `codex mcp login notion`으로 Notion MCP OAuth 인증을 완료했다.
- `codex mcp list`에서 `notion` 서버가 `enabled`, 인증 방식이 `OAuth`로 표시되는 것을 확인했다.
- `git status --short` 확인 결과 기존 작업 변경이 다수 있어, 해당 변경을 포함하지 않도록 커밋과 push는 수행하지 않았다.

## 2026-07-16 (교통량 이상 요청 목록 Excel 정리)

- Notion의 `교통량 이상 요청 목록` 26건을 조회해, 다중 선택된 교차로를 기준으로 46개 행으로 분리했다.
- `이상 발생일`, `조치 요청일`, `조치 완료일`, `이상 교차로`, `유형`, `조치내용` 순서로 정리하고, 원본에서 확인할 수 없는 값은 빈칸으로 유지했다.
- 헤더 고정, 필터, 날짜 `yyyy-mm-dd` 표시 형식, 줄바꿈과 열 너비를 적용한 `교통량_이상_요청목록_교차로별.xlsx`를 생성했다.
- 생성 파일을 다시 열어 6개 헤더, 46개 데이터 행, 필터 테이블 1개를 확인하고 미리보기로 레이아웃을 점검했다.
- 사용자 요청에 따라 헤더·행의 색상 강조를 모두 제거하고, 필터·헤더 고정·날짜 형식은 유지한 새 파일로 바탕화면 결과를 교체했다.
- 필터 드롭다운을 활성화하고 조치 요청일, 이상 발생일, 이상 교차로 순으로 오름차순 정렬한 결과를 생성·재개봉 검증했다. 바탕화면 파일은 다른 프로세스에서 열려 있어 교체 저장이 보류됐다.
- 바탕화면 파일을 닫은 뒤 필터·정렬 적용 버전으로 정상 교체했다.
- 기존 작업 변경이 다수 있어, 이번 로그 변경도 별도 커밋 및 push하지 않았다.

## 2026-07-20 (삼정동 레미콘 지역명 14 포함 산출)

- `docs/rules` 문서와 스프레드시트 작업 지침을 확인하고, SQLite DB를 Python `sqlite3` 읽기 전용 URI(`mode=ro`)로만 조회했다.
- `01_Program\09_else\export_samjeong_remicon_area14_vehicle_by_location.py`를 실행해 `^[가-힣]{2}14[가-힣][0-9]\*{3}$` 형식의 차량 47,935건을 15개 교차로별 시트로 다시 생성했다. 각 시트에는 원본 식별·수집·장비·차로·차량번호·등록지역·원본 파일/시트 컬럼, 필터, 헤더 고정과 헤더 서식을 적용했다.
- `fill_samjeong_remicon_traffic_template.py`의 레미콘 판정 기준을 기존 `^014[가-힣]`와 지역명 포함 14 형식의 합집합으로 확장했다. 기존 `삼정동_레미콘_교통량.xlsx`를 복사해 `삼정동_레미콘_교통량_지역명14포함.xlsx`을 생성하고 `작성서식` 집계를 갱신했다.
- 월별 집계에는 5월·6월 모든 날짜를 포함하고, 평일·첨두시 집계에서는 토·일 및 `2026-05-01`, `2026-05-05`, `2026-05-25`, `2026-06-03`을 제외했다.
- `03_Test\verify_samjeong_remicon_region14_outputs.py`로 DB 재집계, 기존 총 통행량 보존, 상세 시트의 형식·정렬·번호판 형식·총 건수를 검증했다. `traffic_db_reaggregation=matched`, `totals_preserved=matched`, `detail_workbook=matched`를 확인했다.
- 대상 Python 파일에 대해 `py_compile`, `ruff format --check`, `ruff check`를 통과했다. 기존 작업 변경과 분리해 관련 코드·검증 파일만 커밋했으며, 원격 push는 제한 시간 초과로 완료 여부를 확인하지 못했다.

## 2026-07-20 (삼정동 레미콘 지역명 14 포함 일평균·로그 갱신)

- `01_Program\09_else\update_samjeong_remicon_region14_workbook.py`를 추가해 SQLite DB를 `mode=ro`로만 재집계하고 기존 `삼정동_레미콘_교통량_지역명14포함.xlsx` 파일을 갱신했다.
- `월별 일평균` 시트에 5월·6월 8개 교차로 묶음별 `월별`, `평일`, `평일 첨두시`의 분모, 총·레미콘 통행량 일평균, 레미콘 비율을 기록했다. 분모는 월별 31일/30일, 평일·첨두시는 주말 및 지정 공휴일 제외 기준이며 자동차검사소 5월은 20~25일 추가 제외로 월별 25일·평일/첨두시 15일을 적용했다.
- 기존 15개 카메라 로그 시트의 11개 원본 확인 컬럼과 서식을 유지하고, `^014[가-힣]` 및 `^[가-힣]{2}14[가-힣][0-9]\*{3}$` 차량을 중복 없이 합쳐 `collected_at`, `id` 오름차순으로 다시 기록했다. 합산 로그는 31,445건이다.
- `py_compile`, `ruff format`, `ruff check`, `--verify-only` 검증을 통과했다. 검증은 새 시트의 48개 행을 DB 재집계 값과 대조하고, 15개 로그 시트의 행 수·필터·번호판 형식·정렬을 확인했다.

## 2026-07-20 (삼정동 스마트교차로-엣지 비교 작성서식)

- 프로젝트·월간 작업 로그 및 대상 데이터/프로그램 디렉터리 목록을 확인했다.
- 대상 OneDrive Desktop 경로를 확인했으나 현재 경로에는 파일이 없어, 실제 서식 파일 위치를 추가 확인하기로 했다.
- 기존 레미콘 작성서식 채움·검증 구현을 확인했고, 비교 서식 파일명 검색은 Windows 경로 인용 오류로 완료하지 못했다.
- `rg` 실행 파일이 현재 셸 경로에 없어, 이후 코드 검색은 PowerShell의 `Select-String`으로 대체한다.
- `traffic_api`의 Oracle 원시 교통량 조회 구조와 비교 대상의 실제 서식 파일 위치를 확인했다. OneDrive Desktop에는 현재 일치 파일이 없고, 프로젝트 데이터 폴더의 `삼정동_레미콘_교통량_작성서식.xlsx`를 원본 후보로 확인했다.
- 기존 Oracle 분석 모듈의 접근로·방향 및 `S_CRSRD_DRCT_TRF_1HH` 원시 교통량 조회 구현을 확인했다.
- 원시 SQLite/Oracle 읽기 어댑터와 순수 일평균 집계 UseCase를 `samjeong_smart_edge_compare.py`에 추가했다. Oracle 연결은 `SET TRANSACTION READ ONLY`로 제한한다.
- 문법 검사는 통과했으나, 샌드박스 Oracle 접속은 소켓 권한 오류(`WinError 10013`)로 차단되어 승인된 네트워크 권한으로 재시도한다.
- 승인된 읽기 전용 Oracle 조회로 실제 교차로명을 확인해 박촌교 스마트교차로 명칭을 `박촌교삼거리`로 보정했다. 대상 6개 노드의 실제 명칭도 확인했다.
- 접근로 원시명까지 대조해 6개 방향 매핑을 확정하고, 5·6월 12개 행의 엣지/스마트교차로 월별·평일·첨두시 일평균을 정수 반올림으로 계산했다. 스마트교차로 6월 월별 분모는 박촌교 26일, 나머지 5개 교차로 27일이며, 평일·첨두시 분모는 21일이다.
- 원본 레이아웃을 복사한 뒤 대상 행(5월 4~9행, 6월 12~17행)의 C:H 값만 갱신하고 DB 재집계와 워크북 값을 대조했다.
- LibreOffice 실행 파일은 현재 환경에서 확인되지 않아, 별도 렌더링 기반 시각 점검은 수행할 수 없다.
- 단위 테스트 5건, `ruff format`, `ruff check`를 통과했다.
- 완성본을 `C:\Users\An JaeYeol\OneDrive\Desktop\삼정동_교차로_비교_작성서식.xlsx`로 복사했고, 프로젝트 결과본과 SHA-256 해시가 일치함을 확인했다.
- 비교 실행기와 단위 테스트만 `feat(traffic): add samjeong smart-edge comparison` 커밋(`dc573d7`)으로 `origin/develop`에 push했다. 기존 작업트리 변경과 선행 로그 변경은 포함하지 않았다.
- 사용자 요청에 따라 결과 경로를 OneDrive Desktop이 아닌 `00_Data\삼정동_교차로_비교_작성서식.xlsx`로 변경했다. 이 파일의 `작성서식` 시트 C4:H15 12개 값 셀만 제자리에서 갱신하고, 월·교차로명 레이아웃과 값 검증을 다시 수행했다.

## 2026-07-20 (삼정동 레미콘 지역명 14 포함 일평균 서식 보정)

- `월별 일평균` 시트를 새 7열 집계표 대신 `작성서식`을 복제한 `A1:K21` 레이아웃으로 재생성했다. 병합 범위·행/열·안내 문구를 유지하고, 5월·6월 8개 교차로 묶음의 월별·평일·평일 첨두시 총 통행량과 레미콘 통행량 일평균을 소수점 둘째 자리로 기록했다.
- 레미콘 비율은 각 기준의 레미콘 통행량 ÷ 총 통행량 × 100으로 기록했으며, 자동차검사소 5월의 점검 제외 분모(월별 25일, 평일·첨두시 15일)를 유지했다.
- 기존 15개 상세 시트는 원본 확인 11개 컬럼·헤더·서식을 보존한 상태에서 `^014[가-힣]`와 `^[가-힣]{2}14[가-힣][0-9]\*{3}$` 번호판을 합쳐 중복 없이 `collected_at`, `id` 오름차순으로 다시 기록했다. 총 31,445건과 각 필터 범위를 확인했다.
- `py_compile`, `ruff format --check`, `ruff check`, `--verify-only`를 통과했다. 최종 검증은 템플릿 병합 범위와 레이블, 48개 기준별 DB 재집계 일평균·비율, 숫자 표시 형식, 15개 상세 시트의 정렬·번호판 형식·행 수를 대조했다.

## 2026-07-21 (삼정동 레미콘 월별 일평균 수식 채움)

- `02_Result\09_기타\삼정동_레미콘_교통량_v2.xlsx`의 `월별 일평균` 시트 C4:K19 빈 영역을 `작성서식` 참조 수식 144개로 채웠다.
- 통행량 칸은 5월 31일, 6월 30일을 분모로 `ROUND(..., 0)` 처리하고, 5월 자동차검사소 행만 25일을 적용했다. 비율 칸은 채워진 일평균 통행량을 기준으로 `ROUND(레미콘/총*100, 2)` 수식을 적용했다.
- 5월·6월 각 8개 지점 및 3개 통행량 구간의 수식 48묶음과 표시 형식(통행량 `#,##0`, 비율 `0.00`)을 재개봉 검증했다.

## 2026-07-21 (1HH 기반 스마트교차로 비교값 재산정)

- `docs/rules`와 스프레드시트 작업 지침을 확인하고, `S_CRSRD_TRF_1HH`만 사용하도록 스마트교차로 비교 집계기를 변경했다.
- 교차로·일자·시간별로 중복 레코드를 합산한 뒤 `NULL`·0·누락 시간을 무효로 보고, 24시간 중 무효가 4개 이상인 날짜를 모든 지표에서 제외하도록 했다.
- 월별·평일은 유효 날짜의 24시간 합계 일평균으로, 평일 첨두시는 07·08·17·18시의 양수 시간만 사용한 일별 평균의 산술평균으로 계산한다. 평일 기준에서는 지정 공휴일을 제외한다.
- 0~3개와 4개 이상 시간 무효 날짜, 공휴일 제외, 부분 첨두시 분모, 첨두시가 전부 무효인 날짜 제외, 최종 반올림을 검증하는 회귀 테스트 9건을 통과했다.
- Oracle 읽기 전용 재집계로 `삼정동_레미콘_교통량_v2.xlsx`의 `스마트교차로 비교` D/G/J 36개 값을 갱신하고, 대상 셀 값·`#,##0` 형식과 비대상 열의 수식·값 보존을 재검증했다.
- 관련 코드와 회귀 테스트만 `fix(traffic): recalculate smart comparison from 1hh` 커밋(`6c90452`)으로 `origin/develop`에 push했다. 기존 작업트리의 다른 변경과 결과 워크북은 포함하지 않았다.

## 2026-07-21 (스마트교차로 비교값 채움)

- `S_CRSRD_TRF_1DD`와 `S_CRSRD_TRF_1HH`를 Oracle 읽기 전용 트랜잭션으로 조회해, `M_CRSRD_INF`의 6개 교차로 `NODE_ID`를 확인한 뒤 2026년 5월·6월 값을 재집계했다.
- 일별 합계가 0보다 큰 날짜만 분모에 포함하고, 평일·첨두시 집계에서는 토·일과 2026-05-01, 2026-05-05, 2026-05-25, 2026-06-03을 제외했다. 첨두시는 07·08·17·18시 원본 시간별 합계를 사용했다.
- `02_Result\09_기타\삼정동_레미콘_교통량_v2.xlsx`의 `스마트교차로 비교` 시트 D/G/J 열 36개를 정수 반올림값과 `#,##0` 표시 형식으로 갱신했다. 엣지카메라 수식과 비율 열은 전후 스냅샷 비교로 변경되지 않았음을 확인했다.
- `samjeong_smart_edge_compare.py`의 원본 테이블 재집계와 0일·공휴일 분모 제외 회귀 검증을 추가했고, `ruff format --check`, `ruff check`, `pytest` 6건을 통과했다.

## 2026-07-21 (삼정동 레미콘 평일 첨두시 일평균 재산정 준비)

- `docs/rules`와 스프레드시트 작업 지침을 확인하고, SQLite를 URI `mode=ro`로만 조회하는 `01_Program\09_else\recalculate_samjeong_remicon_peak_daily_average.py` 및 단위 테스트를 추가했다.
- 2026년 5~6월의 평일(지정 공휴일 제외) 07·08·17·18시를 교차로별로 집계해, 0인 시간과 0인 일자 제외, 최종 단계 소수점 버림, 정확한 두 레미콘 번호판 형식, I:K 4~19행 외 셀 값·수식 스냅샷 검증을 구현했다.
- `py_compile`, `ruff format`, `ruff check`, `pytest 03_Test\test_recalculate_samjeong_remicon_peak_daily_average.py -q`를 통과했다. 대상 통합 문서는 다른 프로그램의 쓰기 잠금으로 저장이 거부되어, 잠금 해제 후 반영 및 `--verify-only` 검증이 필요하다.
- 잠금 해제 후 `월별 일평균` 시트 I4:K19의 48개 셀만 갱신하고, `--verify-only`로 SQLite 독립 재집계 값·통행량 `#,##0`·비율 `0.00` 형식·비대상 셀 값과 수식 보존을 재검증했다. 5월 총/레미콘 유효 일자 수는 행별 18/18, 18/18, 18/18, 18/17, 18/17, 18/18, 14/14, 18/18이며, 6월은 21/21, 21/21, 21/21, 21/21, 21/20, 21/21, 20/20, 21/21이다.
## 2026-07-22 (삼정동 레미콘 교통량 수정 Sheet1 채우기)

- `docs/rules`와 스프레드시트 작업 지침을 확인하고, `00_Data\삼정동_레미콘\삼정동_레미콘_데이터.db`를 Python `sqlite3` URI의 읽기 전용 모드(`mode=ro`)로만 집계했다.
- `01_Program\09_else\fill_samjeong_remicon_sheet1.py`로 `삼정동_레미콘_교통량_수정_v1.xlsx`의 `Sheet1` 5·6월 30개 설치방향 행 F:N을 채웠다. 모든 방향별 DB 위치를 그대로 대응시키고 `삼정동320-1 부천IC`는 양식의 `삼정동 320-1` 행에 반영했다.
- 레미콘은 `^014[가-힣][0-9]\*{3}$` 및 `^[가-힣]{2,}14[가-힣][0-9]\*{3}$` 번호판 정규식으로 판별했다. 월별 일평균은 5월 31일·6월 30일, 평일 및 평일 첨두시는 휴일을 제외한 5월 18일·6월 21일을 분모로 적용했으며, 첨두시는 07·08·17·18시 기록만 포함했다.
- `ruff format --check`, `ruff check`, DB 재집계 `--verify-only`, 그리고 30개 행 270개 값의 공란·총량/레미콘량·비율·표시형식·D:E 공란·병합 보존 검증을 통과했다. 렌더링으로 헤더, 병합, 테두리, 열 너비 및 숫자 표시 형식을 확인했다.

## 2026-07-22 (삼정동 레미콘 교통량 수정 다방향 합계 행)

- `삼정동_레미콘_교통량_수정_v1.xlsx`의 `Sheet1`에 5월·6월 각각 박촌교 삼거리, 봉오고가교 사거리, 삼정고가 삼거리, 산업길 사거리, 봉오대로 사거리의 `합계` 행을 추가해 총 10개 행을 구성했다.
- 합계 행의 F:G, I:J, L:M에는 방향 행을 범위로 하는 `SUM` 수식을, H·K·N에는 총량 0을 방지하는 비율 수식과 `0.00` 표시 형식을 적용했다. D:E는 비워 두고, 굵은 글꼴과 상단 테두리를 적용했다.
- `fill_samjeong_remicon_sheet1.py`를 고정 행 번호 없이 월·교차로·설치방향을 읽어 행 삽입, 병합 범위 재구성, 합계 수식 생성을 수행하도록 보완했다. 재실행 뒤에도 합계 행은 10개로 유지됨을 확인했다.
- `ruff format`, `ruff check`, `--verify-only`를 통과했다. 검증은 대상 10개 합계 행, 단일 방향의 미삽입, D:E 공란, 수식 범위, 숫자 형식, 굵은 글꼴, 상단 테두리 및 월·교차로 병합 범위를 확인한다.

## 2026-07-22 (삼정동 레미콘 합계 행 오류 표시·테두리 보정)

- 합계 수식은 인접 설치방향 수식과 패턴이 달라 Excel의 일관되지 않은 수식 검사에서 초록 삼각형이 표시되는 것을 확인했다. 합계 수식 셀 범위에 한해 해당 검사 표시를 무시하도록 통합문서 XML을 보완했다.
- 합계 행의 상단 테두리를 인접 방향 행과 동일한 일반 가는 선으로 되돌렸다. 10개 합계 행의 상단 테두리가 모두 `thin`이고, 초록 삼각형 억제용 `ignoredError` 범위가 저장됨을 확인했다.

## 2026-07-22 (자동차검사소 5월 일평균 분모 보정)

- `fill_samjeong_remicon_sheet1.py`에서 5월 자동차검사소만 월 전체 일평균 분모를 25일, 평일 및 평일 첨두시 분모를 15일로 적용했다. DB 집계 분자와 다른 지점·6월의 기본 분모는 유지했다.
- 임시 통합문서에서 자동차검사소 5월 22행의 F:G, I:J, L:M이 각각 DB 집계값 ÷ 25일, ÷ 15일, ÷ 15일과 일치하고, H·K·N 비율 및 숫자 표시 형식이 유지되는 것을 확인했다. 원본과의 비교에서도 22행 외의 값은 변하지 않았고, `--verify-only`, `ruff format --check`, `ruff check`을 통과했다.

## 2026-07-22 (삼정동 레미콘 교통량 수정 Sheet2 스마트교차로 채우기)

- `fill_samjeong_remicon_sheet2_smart.py`를 추가해 Oracle을 `SET TRANSACTION READ ONLY` 세션으로 조회하고, `M_CRSRD_INF`·`M_CRSRD_ACSR_INF`의 교차로·접근로를 각각 하나로 해석한 뒤 `S_CRSRD_ACSR_TRF_1HH`의 `TRF_QNTY`를 집계하도록 했다.
- `삼정동_레미콘_교통량_수정_v1.xlsx`의 `Sheet2` 스마트교차로 24개 방향 행 G/I/K에 2026년 5·6월 일평균·평일·평일첨두 값을 반올림 정수와 `#,##0` 형식으로 반영했다. 고정 분모(월별 31/30일, 평일 18/21일), 지정 공휴일 제외, 07·08·17·18시 첨두 기준을 적용했고 누락 시간·날짜는 0으로 집계했다.
- 방향이 둘 이상인 10개 합계 행에는 해당 방향 행을 더하는 `SUM` 수식을 기록했다. 대상 G/I/K 외 값·스타일 및 병합 범위 보존 검증을 단위 테스트에 추가했다.
- `ruff format --check`, `ruff check`, `pytest 03_Test\test_fill_samjeong_remicon_sheet2_smart.py -q`, Oracle 재집계 `--verify-only`를 모두 통과했다.

## 2026-07-22 (스마트교차로 비교 합계값 표시 보정)

- 통합문서에서 시트명이 `스마트교차로 비교`로 변경된 것을 반영했다. 수식 캐시가 없어 Excel 외 뷰어에서 `-`로 표시되던 합계 G/I/K 셀은 각 방향 행의 합산 정수값으로 직접 기록하도록 변경했다.
- Oracle 읽기 전용 재집계, 단위 테스트 3건 및 `ruff check`를 통과했다.
# 2026-07-22 (v2 스마트교차로 비교값 전체 재계산)

- `samjeong_smart_edge_compare.py`가 `S_CRSRD_ACSR_TRF_1HH`에서 대상 교차로와 접근로를 읽기 전용으로 집계하도록 보정했다.
- 2026년 5~6월의 월 일평균, 평일 일평균, 평일 첨두시 일평균을 각각 31/30일 및 18/21일 고정 분모와 `ROUND_HALF_UP`으로 계산해 `스마트교차로 비교` 시트 D/G/J 36개 셀에 기록했다.
- 월 경계 회귀, 공휴일·첨두시 필터·분모·반올림 및 대상 외 값/수식/스타일 보존 단위 테스트를 통과했다. Oracle 원천 읽기 전용 대조도 통과했다.

# 2026-07-22 (수정_v1 스마트교차로 비교값 채움)

- `삼정동_레미콘_교통량_수정_v1.xlsx`의 `스마트교차로 비교` 시트에서 비어 있던 접근로별 스마트교차로 G/I/K 값을 `S_CRSRD_ACSR_TRF_1HH` 원천으로 다시 집계해 채웠다.
- 월 일평균은 31/30일, 평일과 평일 첨두시 일평균은 공휴일 제외 18/21일 분모를 적용했다. 합계 행과 대상 외 값·수식·서식 보존을 검증하고, Oracle 읽기 전용 재대조도 통과했다.
