# 2026년 7월 작업 로그

## 2026-07-07

- `01_Program\00_main\차종별_교차로_추출.py` 실행 시 DB 환경변수가 로드되지 않는 원인을 확인했다.
- 스크립트는 `01_Program\00_Data\.env`를 참조하지만 실제 `.env`와 `VKND_KIND.json`은 프로젝트 루트의 `00_Data`에 있는 것을 확인했다.
- 루트 `00_Data\.env`에는 `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_SERVICE` 키가 존재하는 것을 값 출력 없이 확인했다.
- `01_Program\00_main\차종별_교차로_추출.py`의 `.env`, `VKND_KIND.json` 참조 경로를 프로젝트 루트 `00_Data` 기준으로 수정했다.
- `.venv\Scripts\python.exe -m py_compile`로 문법 검사를 수행했고, 수정된 경로에서 필수 DB 환경변수 4개가 로드되는 것을 값 출력 없이 확인했다.
