# 9월 작업 로그

## 2026-09-18

- `차종_미확인_비율_분석.py` 실행 결과가 0건인 사례를 생성 XLSX와 파서로 확인했다. 대상 입력은 한 건만 지원하는데 쉼표로 두 대상을 함께 입력해, 두 교차로 문구 일부가 결합된 존재하지 않는 단일 교차로·방향 조건으로 조회됐다. DB 재조회는 수행하지 않았다.

- `차종_미확인_비율_분석.py` XLSX에서 기간을 날짜 전용으로 표시하고, 선택 시간 슬롯을 연속 구간으로 압축한 `시간대`를 조건 영역과 8열 집계표에 분리해 표시하도록 변경했다. `ruff format/check` 및 전용 pytest 3건을 통과했다.

- 차종 미확인 비율 XLSX 분석기의 기간·대상·시간 파서, SQL 읽기 전용 가드, 1시간 차종 원천 집계, 날짜 청크 병렬 합산 및 단일 시트 XLSX 레이아웃을 구현했다. 운영 DB에 계획의 축약 테이블명은 없어 기존 추출기가 사용하는 `S_CRSRD_VKND_TRF_1HH`를 원천으로 사용했다.
- 신규 프로그램을 `init.py` 메뉴에서 테스트 파일 없이 항상 마지막 항목으로 보이게 했고, 전용 단위 테스트 9건 및 대상 파일 Ruff format/check를 통과했다.
- 최신 5일 전체 교차로·07~09시 1~5 워커 읽기 전용 벤치마크를 시작했으나 실행 환경의 30초 명령 제한으로 1워커 결과 출력 전에 중단되어, 보수적인 기본값 1워커를 유지했다. `--benchmark` 옵션으로 동일 기준을 재실행할 수 있다.
- 전체 Ruff 검사는 기존 `get_traffic.py`, `09_else/이상탐지.py` 등 19개 위반으로 실패했고, 전체 pytest는 기존 `09_else/test_vehicle_output_format.py`가 이동된 파일을 참조해 수집 단계에서 실패했다. 전용 검사에는 영향이 없다.
- 관련 코드·테스트·작업 로그만 `feat(analysis): add unidentified vehicle ratio workbook`으로 커밋하고 `develop` 원격 추적 브랜치에 push했다.

- 프로젝트 DB·터미널·아키텍처·Git 규칙, API 원천 XLSX 추출 절차와 기존 차종별 원천 15분 추출기·테스트를 확인했다.
- `차종별_교차로_추출.py`에 고강지하차도사거리의 2026-09-04 오전·오후 첨두 16개 15분 슬롯을 모든 접근로·회전류·차종 원천 행 그대로 단일 XLSX로 저장하는 `--gogang-peak-raw-15m` 옵션을 추가했다. SQL은 `S_CRSRD_VKND_TRF_15MI`에서 집계·교통량 필터링 없이 조회하며, 원천 `VKND_CD` 문자열과 접근로 정보를 보존한다.
- 전용 SQL의 원천 테이블·날짜·16개 슬롯·교차로·차종 코드·비집계 조건과 XLSX의 단일 시트·요청 헤더·차종 코드 문자열을 단위 테스트로 추가했다. 대상 파일 Ruff format/check와 전용 pytest 14건을 통과했다.
- 읽기 전용 Oracle 조회로 `02_Result/차종별_교통량_추출/고강지하차도사거리_260904_첨두시간_15분_원천.xlsx`를 생성했다. 통합문서에서 단일 시트·지정 헤더·원천 행 1,606건, 2026-09-04, 16개 지정 시간대, 고강지하차도사거리, 4개 접근로, 회전류 및 비어 있지 않은 문자열 차종 코드 9종을 검증했다.
- 관련 코드·테스트·작업 로그 3개 파일만 `feat(export): add Gogang peak raw workbook`으로 커밋하고 `develop` 원격 추적 브랜치에 push했다.

- `api-traffic-extract` 절차와 프로젝트 DB·터미널·아키텍처·Git 규칙, 기존 차종별 교차로 원천 15분 추출기 및 테스트를 확인했다.
- `차종별_교차로_추출.py`에 고강지하차도사거리와 같은 두 날짜·16개 15분 슬롯으로 지정 10개 교차로의 모든 접근로 원천 행을 조회하는 XLSX 전용 옵션을 추가했다. 대상 교차로는 조회 전에 모두 검증하며, 원천 `VKND_CD`와 교통량을 집계·수량 필터링 없이 교차로별 시트에 저장한다.
- 전용 SQL과 XLSX 시트·헤더·차종 코드·빈 시트 보존 단위 테스트를 추가했다. 변경 파일 Ruff format/check 및 전용 pytest 12건, `git diff --check`를 통과했다.
- 실행 중인 로컬 API의 OpenAPI를 확인했으나 `raw-traffic-vknd` 경로가 없어 API 추출 규칙에 따라 실제 원천 XLSX 생성은 수행하지 않았다.
- 관련 변경 3개 파일만 `feat(export): add ten intersection raw workbook`으로 커밋하고 `develop` 원격 추적 브랜치에 push했다.
- 사용자 요청에 따라 읽기 전용 원천 조회를 실행해 `02_Result/차종별_교통량_추출/지정10개교차로_260904_260917_15분_원천.xlsx`를 생성했다. 10개 시트와 지정 헤더, 두 날짜·16개 15분 슬롯, 시트별 교차로 일치, 차종 코드 문자열 및 원천 행 21,825건을 검증했다.
- 프로젝트 규칙·간결 구현 지침·작업 로그와 루트 파일 목록을 확인했다.
- `차종별_교차로_추출.py`와 기존 단위 테스트 위치를 확인했다.
- 고강지하차도사거리 전용 원본 15분 SQL·CSV 내보내기와 해당 테스트의 현재 구조를 확인했다.
- 전용 SQL·행 정규화·CSV 저장 함수의 정확한 수정 위치를 확인했다.
- 고강지하차도사거리 전용 원본 15분 CSV에 원천 `VKND_CD`를 차종명 다음 열로 보존하도록 SQL·행 정규화·단위 테스트를 수정했다.
- 작업 트리에 다수의 기존 사용자 변경 사항이 있음을 확인하고, 이번 작업의 전용 추출기·테스트·로그만 후속 검증 및 커밋 대상으로 분리했다.
- 전용 추출기와 단위 테스트 파일의 Ruff 정적 검사를 통과했다.
- 고강지하차도사거리 전용 단위 테스트 10건을 통과해 SQL의 `VKND_CD` 포함과 CSV 헤더·값 보존을 확인했다.
- 읽기 전용 Oracle 연결로 고강지하차도사거리 서측 동향 원본 15분 CSV를 다시 생성했으며 628개 행을 저장했다.
- 재생성 CSV의 UTF-8 BOM, 7개 헤더 순서, 628개 행, 두 지정 날짜·16개 15분 시간대·고강지하차도사거리·서측 조건과 빈 값 없는 차종 코드 9종을 검증했다.
- 변경 diff를 검토해 전용 CSV 흐름에만 `VKND_CD` 열이 추가되고 공용 CSV 형식은 변경되지 않음을 확인했다.
- 전체 Ruff 검사는 이번 변경과 무관한 기존 `get_traffic.py`·`09_else/이상탐지.py` 등의 19개 오류로 실패했으며, 전용 변경 파일 검사는 통과한 상태다.
- 전체 pytest는 기존 이동된 파일을 참조하는 `01_Program/09_else/test_vehicle_output_format.py` 수집 오류로 중단됐으며, 전용 테스트 10건은 별도로 통과했다.
- 이번 작업 파일의 `git diff --check`를 통과했다.
- `develop` 브랜치에서 전용 추출기·전용 테스트·9월 로그 변경만 단계화해 커밋 및 원격 추적 브랜치 반영을 준비했다.
- 프로젝트 DB·터미널·아키텍처·Git 규칙과 차종별 교차로 추출기·기존 단위 테스트를 확인했다.
- `차종별_교차로_추출.py`에 고강지하차도사거리 서측 원본 15분 차종별 CSV 전용 실행 옵션을 추가했다. SQL은 읽기 전용 가드와 `SET TRANSACTION READ ONLY`를 유지하며, `S_CRSRD_VKND_TRF_15MI`의 지정 날짜·시간대 행을 집계·필터링 없이 저장한다.
- 전용 SQL·CSV 출력 단위 테스트를 추가했고, Ruff 검사와 해당 테스트 10건을 통과했다. 공용 pytest 임시 폴더 권한 문제는 프로젝트 내부 전용 임시 경로로 우회해 확인했다.
- 읽기 전용 Oracle 조회로 `02_Result/차종별_교통량_추출/고강지하차도사거리_서_동향__260904_260917_15분.csv`를 생성했으며 원본 행 수는 628건이다.
- 생성 CSV의 UTF-8 BOM, 6개 헤더, 두 날짜, 16개 15분 슬롯, `서` 방향 및 좌·직·우 표기를 확인했다. 원천에 있던 유턴 행도 원본 행 보존 원칙에 따라 유지했다.

## 2026-09-07

- 프로젝트 터미널·아키텍처·Git 규칙과 CSV 작업 지침을 확인하고, `02_Result/차종별_교통량_추출/대장동공영차고지사거리외10개_260904.csv` 원본 및 대상 폴더를 읽기 전용으로 확인했다.
- 원본이 UTF-8 BOM 장형 CSV(시간·교차로·교차로 방향·방향·차종·교통량)임을 확인하고, 방향 열을 제외한 시간·교차로·차종 기준 합산 스크립트와 `대장동공영차고지사거리외10개_260904_교차로별_차종_통합.csv`를 생성했다. 결과는 1,656행이며, 원본·결과 전체 합계는 모두 134,216대이다.
- 고강지하차도사거리 2026-09-04 07:00 SUV의 원본 방향·회전 9개 값(10, 3, 38, 19, 2, 1, 24, 81, 4) 합계 182대와 결과값을 대조했다. 결과 헤더에는 방향 열이 없고, 중복 시간·교차로·차종 키는 0건임을 확인했다. 생성 스크립트는 Ruff 정적 검사와 형식화를 통과했다.

## 2026-09-03

- 프로젝트·터미널·데이터베이스·아키텍처·Git 규칙 및 인앱 브라우저 검증 절차를 확인하고, 기존 합계 산출물과 관련 API·출력 폴더를 읽기 전용으로 점검했다. 로컬 API OpenAPI에서 `/corrected-traffic-drct` 경로와 요청 형식을 확인한 뒤, 노드 `BC001N0127`·접근로 `ACSR000002`·1시간·00~23시·회전류 `01`/`02`/`03` 조건으로 2026-07-13과 2026-08-10 데이터를 읽기 전용 조회했다.
- 조회 결과를 날짜·시간·회전류별 `corrected_value`로 집계해 좌회전·직진·우회전 각각 24개 시간 값을 확인했다. 기존 모든 회전류 합계 엑셀·HTML은 변경하지 않았다.
- `02_Result/API_추출/내동사거리_북측남향_회전류별_시간별_교통량비교_20260713_20260810.html`을 생성했다. 외부 CDN 없이 SVG 그래프 3개를 세로 배치하고, 7월 13일 파란 실선·8월 10일 빨간 점선, 공통 세로축 0~1,200(200 단위), 결측 구간 분리, 범례 및 날짜·시간·교통량 툴팁을 구현했다. 정적 검증에서 그래프 3개·시리즈 6개·점 144개·각 24시간 데이터 및 내장 스크립트 문법이 모두 정상임을 확인했다. 인앱 브라우저 자동 검증은 현재 세션의 브라우저 연결 설정 오류로 수행하지 못했다.
- 작업 트리 상태와 `api-traffic-extract`·스프레드시트 작업 지침, 프로젝트의 터미널·DB·아키텍처·Git 규칙을 확인했다. 기존의 사용자 변경 사항은 유지한다.
- 실행 중인 로컬 API의 `/openapi.json`을 읽기 전용으로 조회해 `/intersections`, `/corrected-traffic-drct` 경로가 제공됨을 확인하고, `/intersections`에서 내동사거리 노드 `BC001N0127`을 확인했다.
- `/corrected-traffic-drct`를 노드 `BC001N0127`, 북(남향) 접근로 `ACSR000002`, 1시간 단위, 각 날짜의 00~23시 조건으로 읽기 전용 조회했다. `drct_slots`의 모든 회전류 `corrected_value`를 시간별 합산했으며 날짜별 96개 회전류 응답과 24개 시간 행을 확인했다. 결측 시간은 임의로 0으로 보정하지 않도록 처리했다.
- `02_Result/API_추출/내동사거리_북측남향_시간별_교통량비교_20260713_20260810.xlsx`와 독립 실행형 HTML 그래프를 생성했다. 엑셀의 24개 행, 표 헤더, 천 단위 숫자 형식, API 출처와 조회 조건을 점검했고 수식 오류 검색 결과는 0건이었다. HTML은 외부 CDN 없이 SVG 선 그래프·범례·마우스오버 툴팁을 포함한다.
- HTML 그래프의 축을 사용자 요청에 따라 가로축 시간(00:00~23:00), 세로축 교통량(대/시간)으로 변경했다. 두 날짜 시리즈·범례·마우스오버 툴팁과 외부 의존성 없음은 유지했다.
