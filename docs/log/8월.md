# 8월 작업 로그

## 2026-08-10

- `01_Program/00_main/교통량_이상추출.py`의 완료·미완료 Excel 시트에서 자동 필터 범위 설정을 제거하고, 기존 첫 행 고정 위치 `A2`는 유지함. 전용 테스트에서 두 시트의 자동 필터가 비어 있고 헤더 행이 고정되는지 검증을 추가했으며, `.venv\Scripts\python.exe -m ruff check 01_Program\00_main\교통량_이상추출.py 01_Program\00_main\test_교통량_이상추출.py` 및 `pytest 01_Program\00_main\test_교통량_이상추출.py` 결과 정적 검사와 16개 테스트를 모두 통과함.

- `01_Program/00_main/교통량_이상추출.py`에서 Notion 날짜 속성을 날짜 전용은 `date`, 시간 포함 값은 시간대 변환 없이 현지 시각의 `datetime`으로 보존하도록 변경함. 완료·미완료 월 선별은 날짜 기준을 유지하고, 이상발생일 정렬은 같은 날짜에서 시각 오름차순으로 처리함. Excel 날짜 열은 날짜 전용 `yyyy-mm-dd`, 시간 포함 `yyyy-mm-dd hh:mm` 형식과 18 너비로 저장하도록 했으며, 혼합 날짜·시간·빈 값 및 동일 일자 시각 정렬 검증 테스트를 추가함.

- 대상 전용 검사에서 `.venv\\Scripts\\python.exe -m ruff format`, `ruff check`, `pytest --basetemp .pytest-notion-temp 01_Program\\00_main\\test_교통량_이상추출.py -q`를 실행하여 16개 테스트와 정적 검사를 통과함. 전체 `ruff format --check`, `ruff check`는 기존 다른 파일의 형식·린트 위반으로 실패했고, 전체 pytest는 기존 누락 파일 `01_Program/09_else/차종별_교차로_추출.py`로 수집 단계에서 중단됨.

- `01_Program/00_main/교통량_이상추출.py`와 전용 테스트를 추가함. Notion 데이터 소스 페이지네이션 조회 결과에서 완료 목록은 조치 완료일 기준 선택 월, 미완료 목록은 2026년 1월 1일부터 선택 월 말일의 이상발생일 기준으로 선별해 `02_Result/교통량_이상추출/2026년_{월}월_교통량_이상목록.xlsx`에 저장하도록 구성함. 다중 선택값, 날짜 서식, Notion 통신·권한 오류, 빈 결과와 파일 저장 오류의 사용자 메시지 및 종료 코드를 처리함.

- `.venv\\Scripts\\python.exe -m ruff format --check`, `ruff check`, `pytest --basetemp .pytest-notion-temp 01_Program\\00_main\\test_교통량_이상추출.py -q`를 실행하여 13개 테스트와 정적 검사를 통과함.

- Notion 보기 링크에서 확인한 데이터베이스 ID `3b86e6899fbc8003a1efd704239737a8`를 추출기에 고정하고, 실행 시 해당 DB의 `2026년 교통량 이상 요청 목록` 데이터 소스 ID를 Notion API로 먼저 조회하도록 변경함. 데이터 소스 ID를 별도로 찾거나 설정하지 않아도 되며, 동일한 ruff·pytest 검증 13건을 통과함.

## 2026-08-06

- 원본 `기후에너지제공데이터_01~10_교차로*.csv` 10개에서 `교차로`와 `교차로 방향`을 다시 읽어 `outputs/교차로_방향_작업목록.xlsx`를 교체함. 마지막 `-` 뒤의 접근로 방향만 `남(북향)`, `남동(북서향)` 형식으로 정규화하고 회전유형은 제외했으며, 286개 교차로의 1,023개 고유 방향 행을 교차로별로 연속 배치해 교차로 셀을 세로 병합함. 표·필터는 제거하고 헤더 고정·테두리·정렬·열 너비를 유지함. 재열기 검증 결과 방향 중복·회전유형·비어 있지 않은 완료일자는 모두 0건이고, 286개 병합 범위가 각 교차로 방향 행과 일치함을 확인함.

- 기존 교통량 추출 CSV 10개(교차로001~286)를 기준으로 고유 `교차로-방향` 1,012개를 정리한 `outputs/교차로_방향_작업목록.xlsx`를 생성함. 단일 작업목록 시트에 `교차로`, `방향`, 빈 `작업완료일자` 열을 표·필터·헤더 고정 형식으로 구성하고, 286개 교차로 포함·중복 없음·완료일자 1,012칸 공란·수식 오류 없음 및 렌더링 가독성을 검증함.

- Windows 예약 작업 `교통량API서버`를 현재 사용자 `BCS\\An JaeYeol`의 로그인 트리거 작업으로 재등록함. 실행 명령은 `cmd.exe /c "C:\\01_Project\\09_교통량 추출 시스템\\01_Program\\09_else\\start_api_server.cmd"`, 시작 위치는 `C:\\01_Project\\09_교통량 추출 시스템\\01_Program\\09_else`로 설정했으며, 중복 인스턴스는 무시하도록 유지함.

- 작업을 수동 실행하여 FastAPI 서버가 `0.0.0.0:8000`에서 LISTENING 중인 것을 확인함(PID 23832, Python). `http://localhost:8000/openapi.json`도 HTTP 200으로 확인함. 작업은 서버 프로세스를 전경으로 유지하므로 검증 시 상태가 `Running`이며, 마지막 결과 `267009 (0x41301)`은 Windows의 `SCHED_S_TASK_RUNNING` 상태 코드임.

## 2026-08-03

- `00_Data/삼정동_레미콘_교통량_수집.xlsx`의 2~7번 시트(6개 교차로, 12개 방향)를 대상으로 오전·오후 첨두시별 최대값 분석 보고서 `02_Result/레미콘_방향별_첨두시_최대값_분석.xlsx`를 생성함. 근거 데이터의 10개 원시 관측값을 기준으로 최대값·동률·유효 관측값 수를 수식으로 계산하고, 요약 시트가 이를 참조하도록 구성함. `-` 결측값 제외, 삼정고가교삼거리 18:00–19:00 제외, 24개 최대값·동률 발생 시점·수식 오류·표 렌더링 검증을 완료함.

- 레미콘 이동흐름 지도 보고서의 화살표 라벨에서 Excel `#NAME?`이 발생한 것을 확인하여, 조건식 기반 라벨을 직접 참조 수식으로 교체하고 오류 검사를 재수행함.

- `차종별_오전오후_첨두_주간_일평균_260720~260724_지정교차로.xlsx`의 레미콘 12개 접근로 값을 기준으로, 오전·오후 이동흐름 지도 분석 보고서 `레미콘_오전오후_첨두_이동흐름_지도분석_260720~260724.xlsx`를 생성함. 근거 데이터의 순위·비중·요약은 수식으로 구성했고, 렌더링 및 수식 오류 검사를 완료함.

- `차종별_오전오후_첨두_주간_일평균_260720~260724_지정교차로.xlsx`를 기반으로 동일 시트 상단에 KPI·차종 비교 차트를 포함한 대시보드를 구성하고, 상세 표를 재배치·서식화함. 원본 대비 756개 상세 셀 값과 수식을 대조하고 수식 오류 및 렌더링을 검증함.

- `차종별_시간대_교통량_집계_260720~260724.xlsx`를 기준으로 교차로 방향별 차종의 오전(08·09시)·오후(16·17시) 첨두 주간 일평균 결과 파일을 생성하고, 22개 방향·440개 관측값 및 표본 값을 검증함.

- `박촌교삼거리외5개_260720~260724.xlsx`의 교통량 시트를 날짜·시간·교차로·교차로 방향·차종 기준으로 집계하고, 차종별 시간대 교통량 요약 엑셀 파일을 생성 및 검증함.

- `삼정동_차종별_교통량_레미콘포함.xlsx` 양식을 확장해 지정 12개 방향의 오전·오후 첨두 주간 일평균 차종별 교통량 결과를 생성하고, 원본 절삭값·합계/비율 수식·레미콘 공란·서식을 검증함.

- 지정 레미콘 첨두 평균 분석을 위해 스프레드시트 작성 지침 및 프로젝트 데이터베이스·터미널·아키텍처·Git 규칙을 확인하고, 프로젝트·데이터·결과·작업 로그 폴더의 대상 파일을 확인함.

- `@oai/artifact-tool`로 원본 통합문서 구조를 점검하려 했으나 현재 프로젝트 의존성에서 패키지를 찾지 못한 상태를 확인함.

- 프로젝트의 기존 런타임 참조로 스프레드시트 도구를 연결해 `삼정동_레미콘_교통량_수집.xlsx` 대상 4개 시트를 점검하고, 병합된 방향명을 이어 붙여 오전(08·09시)·오후(16·17시) 유효 관측값을 집계함.

- `02_Result/삼정동_레미콘_지정4개교차로_첨두평균_260720_260724.xlsx`를 생성하여 8개 방향의 첨두 평균·표본 수·산정 기준을 기록하고, 수식 오류 검색 및 렌더링으로 서식과 가독성을 검증함.
